---
- name: Install Nginx
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: Copy custom HTML
  template:
    src: index.html.j2
    dest: /var/www/html/index.html

- name: Ensure Nginx is running
  service:
    name: nginx
    state: started
    enabled: yes
